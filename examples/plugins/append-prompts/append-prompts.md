# append-prompts

## 目录职责

Append Prompt 示例。复制本目录到 `~/.spotAgent/plugins/append-prompts/` 后，PromptPanel 会出现 `explain` 与 `sum` trigger；这两个 prompt 都是 `kind: "skill"`，只提交渲染后的普通 prompt，不创建 action binding。

## 子节点

- `plugin.json`：Action manifest。
