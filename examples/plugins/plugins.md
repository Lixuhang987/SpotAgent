# plugins

## 目录职责

本目录放可复制到 `~/.spotAgent/plugins/` 的 Action manifest 示例。

## 子节点

- `example-review/example-review.md`：Plugin Action 示例，绑定 `filesystem` MCP server。
- `append-prompts/append-prompts.md`：Append Prompt 示例，只提交普通 prompt，不绑定 MCP。
